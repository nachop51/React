import{jsx as _jsx}from"react/jsx-runtime";import React,{Children,useEffect,useState}from"react";import{EVENTS}from"../utils/consts";import{match}from"path-to-regexp";import{getCurrentPath}from"../utils/getCurrentPath";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>_jsx("h1",{children:"404"})}){const[currentPath,setCurrentPath]=useState(getCurrentPath());useEffect(()=>{const onLocationChange=()=>{setCurrentPath(getCurrentPath())};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}});let routeParams={};const routesFromChildren=Children.map(children,({props,type})=>{const{name}=type;const isRoute=name==="Route";return isRoute?props:null})?.filter(Boolean);console.log(routesFromChildren);const routesToUse=routes.concat(routesFromChildren??[]);const Page=routesToUse.find(({path})=>{if(path===currentPath)return true;const matcherUrl=match(path,{decode:decodeURIComponent});const matched=matcherUrl(currentPath);if(matched===false)return false;routeParams=matched.params;return true})?.Component??DefaultComponent;return Page!==undefined?_jsx(Page,{routeParams:routeParams}):_jsx(DefaultComponent,{})}